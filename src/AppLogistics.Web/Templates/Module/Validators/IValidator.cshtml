@model ModuleModel
using AppLogistics.Objects;
using System;

namespace AppLogistics.Validators
{
    public interface @Model.IValidator : IValidator
    {
        bool CanCreate(@Model.View view);
        bool CanEdit(@Model.View view);
    }
}
